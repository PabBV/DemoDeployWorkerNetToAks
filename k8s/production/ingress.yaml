apiVersion: networking.k8s.io/v1
kind: Ingress

metadata:
  name: worker-app
  namespace: production

spec:
  ingressClassName: nginx

  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix

        backend:
          service:
            name: worker-app-service
            port:
              number: 80